# Carina — 部署

1. 解压项目。
2. 在有 Node.js 的环境执行 `npm install`。
3. 执行 `npm run build`。
4. 将 `dist/` 部署到 HTTPS 静态网站托管。
5. Safari 打开网址并添加到主屏幕。
6. Quick Entry 地址为 `/quick-entry`，可作为 iOS Shortcut 的打开网址目标。

数据保存在浏览器 IndexedDB。正式使用前请先导出备份。
